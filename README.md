# sector-nine 
This project is developed with HTML ,Css ans JavaScript still in developmen for implementation of java script.

## Getting Started

First, use nvm to set the correct node version

```bash
  nvm use
```

Once you have the correct node version, install the dependencies

```bash
  npm install
```

Then, you are ready to start the project

```bash

to run this project you need to run
sass --watch src/scss/main.scss:dist/css/style.css
```bash
  to run this project you need to run
  sass --watch src/scss/main.scss:dist/css/style.css
```